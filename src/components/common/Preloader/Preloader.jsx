import preloader from './../../../assets/images/spin.svg';

let Preloader = (props) => {
      return <div>
            <img src={preloader} alt="Loading..." />
      </div>
}

export default Preloader;